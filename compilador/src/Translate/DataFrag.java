package Translate;

// Classe que estende a classe Frag e que trata de fragmentos de dados
// (strings).
public class DataFrag extends Frag {
    public String data;
    
    DataFrag(String s) {
	data = s;
    }
}
